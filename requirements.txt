gradio>=5.0,<6.0
pillow>=10.0
pydantic>=2.7
numpy>=1.26
spaces>=0.40.0

# llama.cpp text path. The app runs Nemotron GGUF locally through this runtime.
# Defaults use CUDA llama.cpp on ZeroGPU for the no-API badge path.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
--only-binary llama-cpp-python
llama-cpp-python==0.3.22
huggingface-hub>=0.34.0
hf_transfer>=0.1.4

# FLUX.2 Klein image path. The Gradio image callback is the ZeroGPU workload.
diffusers>=0.36.0
torch>=2.9.0,<2.12
accelerate>=1.12.0
transformers>=4.57.0
safetensors>=0.7.0

# CUDA shared libraries needed by the llama.cpp CUDA wheel on Spaces.
nvidia-cuda-runtime-cu12>=12.4
nvidia-cublas-cu12>=12.4
