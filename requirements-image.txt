# Optional image-model runtime dependencies.
#
# Keep this separate from requirements.txt until the Space is intentionally
# switched from the Pillow fallback to a real image model backend.
diffusers>=0.36.0
torch>=2.9.0
accelerate>=1.12.0
transformers>=4.57.0
safetensors>=0.7.0
