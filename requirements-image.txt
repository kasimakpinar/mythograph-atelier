# Optional image-model runtime dependencies.
#
# These are currently mirrored into requirements.txt for the active FLUX test.
# Remove them from requirements.txt again after the test if rebuild/startup cost
# becomes a problem.
diffusers>=0.36.0
torch>=2.9.0
accelerate>=1.12.0
transformers>=4.57.0
peft>=0.18.0
safetensors>=0.7.0
sentencepiece>=0.2.0
protobuf>=6.31.0
